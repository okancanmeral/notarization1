package com.cashonledger;

import java.io.IOException;

import com.cashonledger.AzureStorage.DownloadBlob;
import com.cashonledger.AzureStorage.UploadBlob;
import javax.swing.JOptionPane;

public class NortarizationApplication {
    public static void main(String[] args) throws IOException {

        /*
         * while (true) {
         * 
         * String selectedNumberString = JOptionPane.showInputDialog(null,
         * "PAss a number");
         * 
         * int selectedNumber = Integer.parseInt(selectedNumberString);
         * 
         * if (selectedNumber == 1) {
         * UploadBlob.upload();
         * } else if (selectedNumber == 2) {
         * DownloadBlob.download();
         * } else {
         * System.out.println("Wrong Number:" + selectedNumber);
         * }
         * 
         * }
         */

    }
}
